{
  "name": "papercut-typescript-example",
  "version": "0.0.0",
  "description": "Example on how to use papercut with typescript",
  "main": "build/index.js",
  "author": "Armando Magalhães",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.7.5",
    "nodemon": "^2.0.12",
    "prettier": "^2.3.2",
    "ts-node": "^10.2.1",
    "typescript": "^4.4.2"
  },
  "dependencies": {
    "@armand1m/papercut": "^1.0.3",
    "tslib": "^2.3.1"
  },
  "scripts": {
    "build": "tsc",
    "dev": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/index.ts",
    "lint": "prettier --check './src/**/*.{tsx,ts}'",
    "lint:fix": "prettier --write './src/**/*.{tsx,ts}'"
  }
}
